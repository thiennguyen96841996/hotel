<%= form_for :relationship, url: relationships_path, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("users.follow"), class: "btn btn-primary" %>
<% end %>
