<div id="mod-form-<%= user.id %>" class="mod" >
  <%= form_for :privileges, url: user_path(user), remote: true,
    html: {method: :patch} do |f| %>
    <%= hidden_field_tag :type, user.handle(4) %>
    <%= f.submit t(user.handle(5)), class: user.handle(6) %>
  <% end %>
</div>
